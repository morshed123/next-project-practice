import React from 'react';

const CardList = ({cardList}) => {
    console.log("cardList", cardList);

    return (
        <div>
            cardList
        </div>
    );
};

export default CardList;